---
import BaseLayout, { type Props as BaseProps } from "@/layouts/BaseLayout.astro";

export interface Props extends BaseProps {}
const { ...restProps } = Astro.props;
---

<BaseLayout {...restProps}>
	<Fragment slot="head">
		<meta name="theme-color" content="#06120b" />
	</Fragment>
	<slot />
</BaseLayout>

<style>
	body {
		background-image: radial-gradient(#11311f, #06120b);
	}
</style>

<script>
	if (location.pathname !== "/") {
		if (!document.referrer || document.referrer === "") {
			location.href = "/";
		}

		const url = new URL(document.referrer);
		if (url.pathname !== "/") {
			location.href = "/";
		}
	}
</script>
